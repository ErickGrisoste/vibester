name: payment-service CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "apps/services/payment-service/**"

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/vibester-payment-service

jobs:
  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: apps/services/payment-service/package-lock.json

      - name: Install dependencies
        working-directory: apps/services/payment-service
        env:
          DATABASE_URL: postgresql://u:p@localhost:5432/db
        run: npm ci

      - name: Type check
        working-directory: apps/services/payment-service
        run: npx tsc --noEmit

      - name: Run unit tests
        working-directory: apps/services/payment-service
        env:
          JWT_SECRET: ci-test-secret
          DATABASE_URL: postgresql://u:p@localhost:5432/db
        run: npm test

  integration-test:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: unit-test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: apps/services/payment-service/package-lock.json

      - name: Install dependencies
        working-directory: apps/services/payment-service
        env:
          DATABASE_URL: postgresql://u:p@localhost:5432/db
        run: npm ci

      - name: Run tests with coverage
        working-directory: apps/services/payment-service
        env:
          JWT_SECRET: ci-test-secret
          DATABASE_URL: postgresql://u:p@localhost:5432/db
        run: npm run test:coverage

  build-and-push:
    name: Build & Push
    runs-on: ubuntu-latest
    needs: integration-test
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Normalize image owner casing
        run: echo "IMAGE=ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/vibester-payment-service" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: apps/services/payment-service
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}

  deploy:
    name: Deploy to k3s
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.VPS_KUBECONFIG }}" > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Normalize image owner casing
        run: echo "OWNER_LC=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

      - name: Deploy
        run: |
          kubectl apply -f apps/services/payment-service/k8s/
          kubectl set image deployment/payment-service \
            payment-service=ghcr.io/${{ env.OWNER_LC }}/vibester-payment-service:${{ github.sha }}
          kubectl rollout status deployment/payment-service --timeout=300s || {
            echo "--- Pod describe ---"
            kubectl describe pod -l app=payment-service
            echo "--- Pod logs ---"
            kubectl logs -l app=payment-service --tail=50 --previous 2>/dev/null || kubectl logs -l app=payment-service --tail=50
            exit 1
          }
