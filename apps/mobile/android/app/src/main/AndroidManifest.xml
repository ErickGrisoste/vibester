<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.INTERNET"/>
  <!-- Câmera do app (pacote camera): foto e vídeo. O microfone só é pedido
       ao entrar no modo vídeo; negado, o vídeo grava sem som. -->
  <uses-permission android:name="android.permission.CAMERA"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <!-- Sem isto, CAMERA faz o Play esconder o app de aparelho sem câmera. O
       app trata a ausência (AppCamera → "Sem câmera" → galeria). -->
  <uses-feature android:name="android.hardware.camera" android:required="false"/>
  <!-- O camera_android_camerax também declara `camera.any` (obrigatória) e
       escrita em armazenamento. A feature vira opcional pelo mesmo motivo
       acima. -->
  <uses-feature
      android:name="android.hardware.camera.any"
      android:required="false"
      tools:replace="android:required"/>
  <!-- Cópia das capturas para o álbum "Vibester" da galeria (pacote gal).
       Foto e vídeo continuam gravando no cache do app; esta permissão é só
       para a cópia, e só em Android 10 ou anterior — do 11 em diante o
       MediaStore dispensa permissão. O camera_android_camerax declara a
       mesma linha com 28, daí o `tools:replace`. -->
  <uses-permission
      android:name="android.permission.WRITE_EXTERNAL_STORAGE"
      android:maxSdkVersion="29"
      tools:replace="android:maxSdkVersion"/>
  <!-- A galeria usa o Photo Picker do sistema (obrigatório no Android 13+),
       que não exige permissão: READ_MEDIA_IMAGES saiu. A leitura de
       armazenamento fica só para Android 12 ou anterior. -->
  <uses-permission
      android:name="android.permission.READ_EXTERNAL_STORAGE"
      android:maxSdkVersion="32"/>
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
   
    <!-- Backup automático desligado: a sessão fica no flutter_secure_storage,
         cifrada com chave do Keystore do aparelho; restaurada em outro
         aparelho, a chave não existe e a leitura quebra. -->
    <application
        android:label="Vibester"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="vibester" android:host="profile"/>
                <data android:scheme="vibester" android:host="event"/>
                <data android:scheme="vibester" android:host="place"/>
            </intent-filter>
        </activity>
        <!-- Recorte da foto de perfil (image_cropper → uCrop). -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <!-- Ajustes → Ajuda e contato (mailto:contato@vibester.com.br). -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>

        <!-- Termos/Privacidade abrem em Custom Tabs (LaunchMode.inAppBrowserView);
             sem esta consulta o url_launcher não enxerga o navegador compatível. -->
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>
</manifest>