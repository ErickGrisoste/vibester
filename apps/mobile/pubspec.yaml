name: mobile
description: "Vibester — descubra eventos, lugares e rolês perto de você."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  pinput: ^5.0.0
  image_picker: ^1.1.2
  intl: ^0.19.0
  diacritic: ^0.1.6
  flutter_map: ^7.0.0
  latlong2: ^0.9.0   
  font_awesome_flutter: ^11.0.0
  provider: ^6.1.5+1
  cached_network_image: ^3.4.1
  # Direta porque o cache de imagem é configurado no app
  # (lib/service/media/image_cache.dart); já vinha via cached_network_image.
  flutter_cache_manager: ^3.4.1


  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.0
  dio: ^5.9.2
  email_validator: ^3.0.0
  geolocator: ^14.0.3
  flutter_secure_storage: ^10.3.1
  share_plus: ^13.3.0
  app_links: ^7.0.0
  shared_preferences: ^2.5.3
  camera: ^0.12.0+2
  image_cropper: ^12.2.1
  flutter_image_compress: ^2.5.1
  video_compress: ^3.1.4
  video_player: ^2.11.1
  gal: ^2.3.3
  visibility_detector: ^0.4.0+2
  uuid: ^4.6.0

dependency_overrides:
  # path_provider_foundation 2.6.0 (assim como a 2.5.0) depende de package:objective_c,
  # cujo objective_c.framework nao tem slice x86_64 - trava com dlopen em qualquer
  # chamada de path_provider no simulador iOS de Mac Intel, e como o flutter_cache_manager
  # (usado pelo cached_network_image) depende de um path_provider valido pra cachear
  # download de imagem, isso trava toda imagem de rede num loading infinito.
  # 2.5.1 nao tem essa dependencia. Remover quando o pacote consertar isso upstream.
  path_provider_foundation: 2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Gera os ícones de app das duas plataformas a partir de assets/icon/.
  # Só roda sob demanda (`dart run flutter_launcher_icons`), não entra no bundle.
  flutter_launcher_icons: ^0.14.4

# Ícone do app. Fonte da arte: design/assets/icone_app.png (mascote sobre o navy
# #041440). `app_icon.png` é a arte cheia, sem alpha, que o iOS usa; o foreground
# adaptativo do Android é só o mascote recortado, preenchendo o canvas — quem o
# reduz para a safe zone da máscara é o inset de 16% do ic_launcher.xml gerado,
# e o navy volta como adaptive_icon_background. Não encolha a arte do foreground
# de novo por cima disso, senão o mascote fica minúsculo na tela.
# Regerar: `dart run flutter_launcher_icons` na pasta apps/mobile. O pacote troca
# ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS por engano no
# project.pbxproj do iOS (match de substring numa chave booleana) — reverta isso.
flutter_launcher_icons:
  image_path: "assets/icon/app_icon.png"
  android: "ic_launcher"
  adaptive_icon_background: "#041440"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  adaptive_icon_monochrome: "assets/icon/app_icon_foreground.png"
  ios: true
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - assets/img/
    # Arte do mascote e fotos de estabelecimentos: continuam empacotadas,
    # embora nenhuma tela do redesenho as use hoje.
    - assets/img/mascote/
    - assets/img/places/
    - assets/img/logo/
  fonts:
    - family: Outfit
      fonts:
        - asset: assets/fonts/Outfit/Outfit-Regular.ttf
          weight: 400
        - asset: assets/fonts/Outfit/Outfit-Medium.ttf
          weight: 500
        - asset: assets/fonts/Outfit/Outfit-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Outfit/Outfit-Bold.ttf
          weight: 700
        - asset: assets/fonts/Outfit/Outfit-ExtraBold.ttf
          weight: 800
    - family: DMMono
      fonts:
        - asset: assets/fonts/DMMono/DMMono-Light.ttf
          weight: 300
        - asset: assets/fonts/DMMono/DMMono-Regular.ttf
          weight: 400
        - asset: assets/fonts/DMMono/DMMono-Medium.ttf
          weight: 500
